{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && gulp build",
    "clean": "gulp clean",
    "watch": "npm run clean && gulp watch",
    "lerna-publish": "npm run clean && learn publish"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-plugin-source-map-support-for-6": "0.0.5",
    "babel-preset-es2015-node4": "^2.0.3",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-watch": "^4.3.8",
    "lerna": "kittens/lerna",
    "rimraf": "^2.5.3",
    "tape": "^4.6.0",
    "through2": "^2.0.1"
  }
}
